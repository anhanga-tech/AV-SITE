blank_issues_enabled: false
# Repo privado: Discussions não usado no momento.
# blank_issues_enabled: false força o uso de um dos formulários (Issue Forms),
# o que mantém issues abertas por humanos OU agentes/LLMs dentro do mesmo padrão.
# Se habilitar Discussions no futuro, adicionar um contact_link aqui.
